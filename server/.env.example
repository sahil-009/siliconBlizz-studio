# Port (default: 5000)
PORT=5000

# Email address that receives contact form submissions
RECIPIENT_EMAIL=sahilmund.6685@gmail.com

# SMTP (e.g. Gmail, SendGrid, Mailgun). For Gmail use App Password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=your-app-password
# Optional: "From" name and address
# SMTP_FROM="R U Interior Design <noreply@yourdomain.com>"
